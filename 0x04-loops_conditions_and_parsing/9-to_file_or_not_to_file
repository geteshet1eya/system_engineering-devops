#!/usr/bin/env bash
# Check if a given path is a readable regular file
file="$1"
if [ -z "$file" ]; then
  echo "Usage: $0 <file>"
  exit 1
fi

if [ -e "$file" ] && [ -f "$file" ] && [ -r "$file" ]; then
  echo "YES"
else
  echo "NO"
fi
