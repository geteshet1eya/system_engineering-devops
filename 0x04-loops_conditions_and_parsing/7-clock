#!/usr/bin/env bash
# Print a 12-hour clock HH:MM and insert BH after 11:59
for h in $(seq -w 0 12); do
  for m in $(seq -w 0 59); do
    echo "$h:$m"
    if [ "$h" = "11" ] && [ "$m" = "59" ]; then
      echo "BH"
    fi
  done
done
