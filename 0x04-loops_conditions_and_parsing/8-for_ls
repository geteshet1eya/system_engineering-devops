#!/usr/bin/env bash
# A Bash script that lists files in the current directory, showing only the part after the first dash

for file in *; do
  # Skip hidden files
  if [[ $file != .* ]]; then
    # Remove everything up to the first dash
    echo "${file#*-}"
  fi
done
